# fix_env.py
def create_env_file():
    print("🔧 Создаем файл .env...")
    
    # Запросите токен у пользователя
    token = input("Введите токен вашего бота: ").strip()
    
    # Удалите возможные кавычки если пользователь их ввел
    token = token.replace('"', '').replace("'", "")
    
    # Запишите в файл
    with open('.env', 'w', encoding='utf-8') as f:
        f.write(f"API_TOKEN={token}\n")
    
    print("✅ Файл .env успешно создан!")
    print(f"📝 Токен сохранен: {token[:10]}...{token[-10:]}")
    
    # Проверим запись
    with open('.env', 'r', encoding='utf-8') as f:
        content = f.read().strip()
        print(f"📄 Содержимое файла: '{content}'")

if __name__ == "__main__":
    create_env_file()